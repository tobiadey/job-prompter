# import os

# Job Prompter Database
# SQLALCHEMY_DATABASE_URI = os.environ["AIRLINK_DB_URL"]
SQLALCHEMY_DATABASE_URI = (
    "postgresql://user:user@localhost/job_prompter_dev"  # will change later
)
